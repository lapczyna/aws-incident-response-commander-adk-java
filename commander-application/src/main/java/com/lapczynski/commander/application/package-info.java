/**
 * Application services and outbound ports.
 *
 * <p>Ports declared here are implemented twice: once against real AWS in commander-integrations-aws
 * and once deterministically in commander-simulator, so the whole workflow runs with no AWS
 * account.
 */
package com.lapczynski.commander.application;
