/**
 * Deterministic, fixture-driven AWS signal simulator.
 *
 * <p>Implements the same ports as commander-integrations-aws so the full incident workflow can be
 * demonstrated and tested reproducibly without an AWS account.
 */
package com.lapczynski.commander.simulator;
